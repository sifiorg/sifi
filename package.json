{
  "name": "sifi",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/sideshiftfi/sifi",
  "author": "sideshift",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "lerna run build --stream",
    "clean": "lerna run clean --stream",
    "test": "lerna run test --stream",
    "lint": "lerna run lint --stream --parallel --no-bail"
  },
  "devDependencies": {
    "@typescript-eslint/parser": "^5.40.0",
    "eslint": "^8.25.0",
    "eslint-config-airbnb-typescript-prettier": "^5.0.0",
    "eslint-import-resolver-workspaces": "^1.2.0",
    "eslint-plugin-workspaces": "^0.7.0",
    "prettier": "^2.7.1",
    "typescript": "^4.9.3"
  }
}
